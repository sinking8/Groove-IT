/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME: "dmr4n68im",
    NEXT_SERVER_URL: "http://127.0.0.1:8000",
  },
};

export default nextConfig;
